---
name: verification-before-completion
description: 在声称工作完成、已修复或通过之前使用 - 需要运行验证命令并确认输出，然后再做任何成功声明；证据优先于断言
---

# 完成前验证

## 概述

不经过验证就声称工作完成是不诚实，而不是提高效率。

**核心原则：**证据优先于声明，总是如此。

**违反字面上的规则就是违反了精神上的规则。**

## 铁律

```
无验证证据不得声称完成
```

如果你还没有在这条消息中运行验证命令，你就不能声称它通过了。

## 门函数

```
在声称任何状态或表示满意之前：

1. 确定：什么命令能证明这个声明？
2. 运行：执行完整命令（全新的、完整的）
3. 读取：完整输出、检查退出代码、计数失败次数
4. 验证：输出是否确认了声明？
   - 如果否：用证据说明实际状态
   - 如果是：用证据声明声明
5. 只有然后：做出声明

跳过任何步骤 = 撒谎，而不是验证
```

## 常见失败

| 声明 | 需要 | 不足够 |
| --------------------- | ------------------------------- | ------------------------------ |
| 测试通过 | 测试命令输出：0次失败 | 前次运行、"应当通过" |
| Linter 清洁 | Linter 输出：0个错误 | 部分检查、推断 |
| 构建成功 | 构建命令：退出 0 | Linter 通过、日志看起来不错 |
| 错误已修复 | 测试原始症状：通过 | 代码已改、假设已修复 |
| 回归测试工作 | 红绿周期已验证 | 测试一次通过 |
| 代理已完成 | VCS 差异显示更改 | 代理报告"成功" |
| 需求已满足 | 逐行检查清单 | 测试通过 |

## 红旗 - 停止

- 使用"应该"、"可能"、"似乎"
- 在验证前表示满意（"太好了！"、"完美！"、"完成！"等）
- 即将提交/推送/PR而未经验证
- 信任代理成功报告
- 依赖部分验证
- 认为"只这一次"
- 疲倦并希望工作结束
- **任何暗示成功但未运行验证的措辞**

## 理性化预防

| 借口 | 现实 |
| --------------------------------------- | ---------------------- |
| "现在应该可以工作" | 运行验证 |
| "我很有信心" | 信心 ≠ 证据 |
| "只这一次" | 无异常 |
| "Linter 通过了" | Linter ≠ 编译器 |
| "代理说成功了" | 独立验证 |
| "我累了" | 疲劳 ≠ 借口 |
| "部分检查足够了" | 部分证明不了什么 |
| "不同的措辞所以规则不适用" | 精神优先于字面 |

## 关键模式

**测试：**
```
✅ [运行测试命令] [查看：34/34 通过] "所有测试通过"
❌ "应该通过现在" / "看起来正确"
```

**回归测试（TDD 红绿）：**
```
✅ 编写 → 运行（通过）→ 还原修复 → 运行（必须失败）→ 恢复 → 运行（通过）
❌ "我写了一个回归测试"（未经红绿验证）
```

**构建：**
```
✅ [运行构建] [查看：退出 0] "构建通过"
❌ "Linter 通过了"（linter 不检查编译）
```

**需求：**
```
✅ 重新阅读计划 → 创建检查清单 → 验证每一项 → 报告差距或完成
❌ "测试通过，阶段完成"
```

**代理委派：**
```
✅ 代理报告成功 → 检查 VCS 差异 → 验证更改 → 报告实际状态
❌ 信任代理报告
```

## 为什么这很重要

来自 24 次失败记忆：
- 你的人类伙伴说"我不相信你" - 信任破裂
- 未定义的函数发货 - 会崩溃
- 缺失的需求发货 - 功能不完整
- 时间浪费在虚假完成 → 重定向 → 返工
- 违反："诚实是核心价值。如果你撒谎，你会被替换。"

## 何时应用

**总是在以下之前：**
- 任何成功/完成声明的变体
- 任何满意的表达
- 任何关于工作状态的积极声明
- 提交、创建 PR、任务完成
- 移到下一个任务
- 委派给代理

**规则适用于：**
- 确切的措辞
- 释义和同义词
- 成功的暗示
- 任何暗示完成/正确性的沟通

## 底线

**验证没有捷径。**

运行命令。读取输出。然后声明结果。

这是不可协商的。
