---
name: finishing-a-development-branch
description: 在实现完成、所有测试通过并需要决定如何集成工作时使用 - 通过呈现结构化选项来指导开发工作的完成，包括合并、拉取请求或清理
---

# 完成开发分支

## 概述

通过呈现清晰的选项和处理所选工作流来指导开发工作的完成。

**核心原则：** 验证测试 → 呈现选项 → 执行选择 → 清理。

**在开始时宣布：** "我正在使用完成开发分支(finishing-a-development-branch)技能(skill)来完成此工作。"

## 流程

### 第1步：验证测试

**在呈现选项之前，验证测试通过：**

```bash
# 运行项目的测试套件
npm test / cargo test / pytest / go test ./...
```

**如果测试失败：**
```
测试失败(<N> 个失败)。必须在完成之前修复：

[显示失败]

在合并/PR通过之前无法继续。
```

停止。不要继续执行第2步。

**如果测试通过：** 继续执行第2步。

### 第2步：确定基础分支

```bash
# 尝试常见的基础分支
git merge-base HEAD main 2>/dev/null || git merge-base HEAD master 2>/dev/null
```

或询问："此分支来自main - 这是正确的吗？"

### 第3步：呈现选项

呈现恰好这4个选项：

```
实现完成。您想做什么？

1. 合并回<基础分支>(本地)
2. 推送并创建拉取请求
3. 保持分支现状(我稍后处理)
4. 放弃此工作

选择哪个选项?
```

**不要添加解释** - 保持选项简洁。

### 第4步：执行选择

#### 选项1：本地合并

```bash
# 切换到基础分支
git checkout <base-branch>

# 获取最新版本
git pull

# 合并功能分支
git merge <feature-branch>

# 在合并结果上验证测试
<test command>

# 如果测试通过
git branch -d <feature-branch>
```

然后：清理工作树(第5步)

#### 选项2：推送并创建PR

```bash
# 推送分支
git push -u origin <feature-branch>

# 创建PR
gh pr create --title "<title>" --body "$(cat <<'EOF'
## 概述
<2-3个更改内容的要点>

## 测试计划
- [ ] <验证步骤>
EOF
)"
```

然后：清理工作树(第5步)

#### 选项3：保持现状

报告："保持分支<名称>。工作树保留在<路径>。"

**不要清理工作树。**

#### 选项4：放弃

**首先确认：**
```
这将永久删除：
- 分支<名称>
- 所有提交：<提交列表>
- <路径>处的工作树

输入'discard'进行确认。
```

等待确切的确认。

如果确认：
```bash
git checkout <base-branch>
git branch -D <feature-branch>
```

然后：清理工作树(第5步)

### 第5步：清理工作树

**对于选项1、2、4：**

检查是否在工作树中：
```bash
git worktree list | grep $(git branch --show-current)
```

如果是：
```bash
git worktree remove <worktree-path>
```

**对于选项3：** 保持工作树。

## 快速参考

| 选项 | 合并 | 推送 | 保持工作树 | 清理分支 |
|--------|-------|------|---------------|----------------|
| 1. 本地合并 | ✓ | - | - | ✓ |
| 2. 创建PR | - | ✓ | ✓ | - |
| 3. 保持现状 | - | - | ✓ | - |
| 4. 放弃 | - | - | - | ✓(强制) |

## 常见错误

**跳过测试验证**
- **问题：** 合并破损代码，创建失败的PR
- **修复：** 在提供选项之前始终验证测试

**开放式问题**
- **问题：** "我接下来应该做什么？" → 模糊
- **修复：** 呈现恰好4个结构化选项

**自动工作树清理**
- **问题：** 删除可能需要的工作树(选项2、3)
- **修复：** 仅对选项1和4清理

**放弃时无确认**
- **问题：** 意外删除工作
- **修复：** 需要输入"discard"确认

## 红旗

**决不：**
- 继续使用失败的测试
- 合并而不验证结果上的测试
- 删除工作而不确认
- 在没有明确请求的情况下强制推送

**始终：**
- 在提供选项之前验证测试
- 呈现恰好4个选项
- 为选项4获取输入确认
- 仅对选项1和4清理工作树

## 集成

**调用者：**
- **子代理驱动开发(subagent-driven-development)**(第7步) - 在所有任务完成后
- **执行计划(executing-plans)**(第5步) - 在所有批次完成后

**配对：**
- **使用Git工作树(using-git-worktrees)** - 清理该技能(skill)创建的工作树
